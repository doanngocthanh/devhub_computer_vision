# devhub_computer_vision
```
sudo mkdir -p /var/lib/apt/lists/partial && sudo apt-get update && sudo apt-get install -y --no-install-recommends tesseract-ocr libtesseract-dev libleptonica-dev pkg-config
```